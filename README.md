#### This repo contains the frontend component of the QuotesApp, which uses HTML, CSS and JavaScript. The app accepts quotes and updates the page with the last 4 quotes (including the newly added quote). 
